
/**
 *
 */
macroscript	maxtoprint_select_by_z_pos
category:	"maxtoprint"
buttontext:	"Sort by Z pos"
toolTip:	""
icon:	"across:4"
(
	on execute do
	(
		--_selection = selection as Array
		--
		--mode = 1	-- *** 1 = accending | 2 = decending ***
		--
		--fn sort_z v1 v2 = (
		--
		--	if (mode == 1) then
		--		result = v1.position.z - v2.position.z
		--
		--	else if (mode == 2) then
		--		result = v2.position.z - v1.position.z
		--)
		--
		--qsort _selection sort_z
		--
		--select _selection
		--
		--for i in _selection do print i
	)
)
